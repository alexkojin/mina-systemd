require 'spec_helper'

describe Mina::Systemd do
  it 'has a version number' do
    expect(Mina::Systemd::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
